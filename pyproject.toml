[project]
name = "cascadia"
version = "0.1.0"
description = "Autonomous Causal Dependency and Truth-Cascade Protocol on GenLayer"
authors = [{ name = "9ja_maxx", email = "297689616+9ja-maxx@users.noreply.github.com" }]
requires-python = ">=3.11"
dependencies = [
  "genlayer-py==0.16.3",
  "genlayer-test==0.29.2",
  "genvm-linter==0.11.0",
  "pytest==9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
